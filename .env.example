# ============================================================
# UI Police — Environment Variables (secrets only)
#
# All non-sensitive configuration (environments, viewports,
# timeouts, apps) lives in ui-police.config.ts at project root.
#
# Credential pattern: {ENVIRONMENT_NAME_UPPER}_EMAIL / _PASSWORD
# The environment names come from ui-police.config.ts.
# ============================================================

# Develop environment credentials
DEVELOP_EMAIL=dev.user@cariloop.com
DEVELOP_PASSWORD=dev-password

# Local environment credentials
LOCAL_EMAIL=local.user@cariloop.com
LOCAL_PASSWORD=local-password

# Auth app: Registration flow mode
# "screenshot-only" = Fill form + screenshot without submitting (default, non-destructive)
# "full-flow"       = Submit registration using a test user from the pool (destructive)
# "disabled"        = Skip registration flow entirely
AUTH_REGISTRATION_MODE=screenshot-only
